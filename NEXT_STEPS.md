# 🎯 Следующие шаги разработки

**Дата:** 2025-11-19  
**Текущий статус:** Production Ready (85%)  
**Приоритет:** Критические задачи → Улучшения

---

## 🚨 Критические задачи (P0-P1)

### 1. TASK-001: Исправление парсинга OfferSubtitle ⚠️ CRITICAL
**Приоритет:** P0 (Critical)  
**Статус:** 🟡 In Progress  
**Effort:** 2 часа

**Проблема:**
- Парсер не извлекает данные из OfferSubtitle когда OfferTitle содержит рекламный текст
- Влияет на качество данных (<90% completeness)

**Действия:**
```bash
# 1. Изучить текущий код
cat etl/collector_cian/browser_fetcher.py | sed -n '224,254p'

# 2. Рефакторинг парсера
# - Проверять OfferSubtitle первым
# - Fallback на OfferTitle
# - Regex для определения рекламного текста

# 3. Написать тесты
# - 3 новых тест-кейса в tests/test_mapper.py

# 4. Интеграционное тестирование
python -m etl.collector_cian.cli to-db --pages 1

# 5. Проверка качества данных
psql -h localhost -U realuser -d realdb -c \
  "SELECT COUNT(*) FROM listings WHERE rooms IS NOT NULL;"
```

**Файлы:**
- `etl/collector_cian/browser_fetcher.py` (строки 224-254)
- `tests/test_mapper.py` (новые тесты)

**Критерии приемки:**
- [ ] Парсер проверяет оба поля (OfferTitle и OfferSubtitle)
- [ ] Предпочитает OfferSubtitle если содержит детали недвижимости
- [ ] Тестовое покрытие: 3 новых теста
- [ ] Completeness >90% после ре-парсинга

---

### 2. BUG-001: Фильтрация долей квартир
**Приоритет:** P1 (High)  
**Статус:** 🔴 Confirmed  
**Effort:** 1 час

**Проблема:**
- В БД попадают доли квартир (area < 20m²)
- 17 записей (1.1%) - доли вместо полных квартир

**Действия:**
```bash
# 1. Добавить фильтр минимальной площади в payload
# Изменить etl/collector_cian/payloads/cheap_first.yaml
# Добавить: minArea: 20

# 2. Очистить существующие доли из БД
psql -h localhost -U realuser -d realdb -c \
  "UPDATE listings SET is_active = FALSE WHERE area_total < 20;"

# 3. Обновить валидацию в mapper
# Добавить проверку area_total >= 20 перед сохранением
```

**Файлы:**
- `etl/collector_cian/payloads/cheap_first.yaml`
- `etl/collector_cian/mapper.py` (валидация)

**Критерии приемки:**
- [ ] Минимальная площадь 20m² в payload
- [ ] Валидация в mapper
- [ ] Существующие доли деактивированы
- [ ] Новые доли не попадают в БД

---

## 📊 Production улучшения (P1-P2)

### 3. TASK-PROD-016: Расширенный мониторинг
**Приоритет:** P1 (High)  
**Статус:** 🟡 In Progress  
**Effort:** 4 часа

**Текущий статус:**
- ✅ Базовая система алертов создана
- ⏳ Нужны дашборды и метрики

**Действия:**
```bash
# Вариант 1: Prometheus + Grafana (рекомендуется)
# 1. Добавить Prometheus в docker-compose.yml
# 2. Настроить экспорт метрик из приложений
# 3. Создать Grafana дашборды

# Вариант 2: Использовать Metabase (уже есть)
# 1. Создать дашборды в Metabase
# 2. Настроить автоматические отчеты
```

**Критерии приемки:**
- [ ] Метрики собираются (Prometheus или Metabase)
- [ ] Дашборды созданы (3+ дашборда)
- [ ] Метрики обновляются в реальном времени

---

### 4. TASK-PROD-021: Database Connection Pooling
**Приоритет:** P2 (Medium)  
**Статус:** 📋 Pending  
**Effort:** 3 часа

**Проблема:**
- Нет connection pooling
- Может привести к проблемам при высокой нагрузке

**Действия:**
```python
# Использовать SQLAlchemy connection pool
# Или psycopg2 pool
from psycopg2 import pool

connection_pool = psycopg2.pool.SimpleConnectionPool(
    1, 20,
    host=host,
    database=database,
    user=user,
    password=password
)
```

**Критерии приемки:**
- [ ] Connection pool настроен
- [ ] Метрики pool мониторятся
- [ ] Производительность улучшена

---

## 🔧 Улучшения качества данных (P1-P2)

### 5. TASK-002: Улучшение извлечения адресов
**Приоритет:** P1 (High)  
**Статус:** 📋 Pending  
**Effort:** 1 час

**Проблема:**
- Некоторые адреса не извлекаются
- Нужны fallback селекторы

**Действия:**
```python
# Добавить множественные селекторы:
selectors = [
    "[data-name='GeoLabel']",
    "[data-name='SpecialGeo']",
    ".geo-label",
    # fallback селекторы
]

# Валидация: адрес должен содержать "Москва" или название метро
```

**Критерии приемки:**
- [ ] Множественные селекторы реализованы
- [ ] Валидация адреса добавлена
- [ ] Логирование пропущенных адресов

---

### 6. TASK-005: Метрики качества данных
**Приоритет:** P2 (Medium)  
**Статус:** 📋 Pending  
**Effort:** 3 часа

**Действия:**
```sql
-- Создать SQL view
CREATE OR REPLACE VIEW data_quality_metrics AS
SELECT 
  COUNT(*) as total_listings,
  COUNT(*) FILTER (WHERE rooms IS NOT NULL) * 100.0 / COUNT(*) as pct_has_rooms,
  COUNT(*) FILTER (WHERE area_total IS NOT NULL) * 100.0 / COUNT(*) as pct_has_area,
  COUNT(*) FILTER (WHERE address IS NOT NULL AND address != '') * 100.0 / COUNT(*) as pct_has_address,
  COUNT(*) FILTER (WHERE description IS NOT NULL) * 100.0 / COUNT(*) as pct_has_description
FROM listings;
```

**Критерии приемки:**
- [ ] SQL view создана
- [ ] Логирование метрик в CLI
- [ ] Метрики доступны в Metabase

---

## 🚀 Автоматизация (P2)

### 7. TASK-004: Автоматический ежедневный сбор данных
**Приоритет:** P2 (Medium)  
**Статус:** 📋 Pending  
**Effort:** 2 часа

**Действия:**
```bash
# 1. Создать systemd service
sudo tee /etc/systemd/system/cian-scraper.service > /dev/null <<EOF
[Unit]
Description=CIAN Data Scraper
After=network.target postgresql.service

[Service]
Type=oneshot
User=ubuntu
WorkingDirectory=/home/ubuntu/realestate
Environment="PATH=/home/ubuntu/realestate/venv/bin"
EnvironmentFile=/home/ubuntu/realestate/.env
ExecStart=/home/ubuntu/realestate/venv/bin/python -m etl.collector_cian.cli to-db --pages 10
StandardOutput=journal
StandardError=journal
EOF

# 2. Создать timer
sudo tee /etc/systemd/system/cian-scraper.timer > /dev/null <<EOF
[Unit]
Description=Run CIAN scraper daily

[Timer]
OnCalendar=*-*-* 03:00:00
Timezone=Europe/Moscow

[Install]
WantedBy=timers.target
EOF

# 3. Активировать
sudo systemctl enable cian-scraper.timer
sudo systemctl start cian-scraper.timer
```

**Критерии приемки:**
- [ ] Systemd service создан
- [ ] Timer настроен (3 AM Moscow time)
- [ ] Тестовый запуск успешен
- [ ] Логи доступны

---

## 📋 Рекомендуемый порядок выполнения

### Неделя 1 (Критическое)
1. **TASK-001** - Исправление парсинга (2 часа) ⚠️ CRITICAL
2. **BUG-001** - Фильтрация долей (1 час) ⚠️ HIGH
3. **TASK-002** - Улучшение адресов (1 час)

**Итого:** 4 часа критических исправлений

### Неделя 2 (Production)
4. **TASK-PROD-016** - Расширенный мониторинг (4 часа)
5. **TASK-005** - Метрики качества данных (3 часа)
6. **TASK-004** - Автоматизация сбора (2 часа)

**Итого:** 9 часов production улучшений

### Неделя 3 (Оптимизация)
7. **TASK-PROD-021** - Connection pooling (3 часа)
8. **TASK-PROD-022** - Caching layer (4 часа) - опционально

**Итого:** 7 часов оптимизации

---

## 🎯 Быстрый старт (сегодня)

### Шаг 1: Исправить критический баг парсинга
```bash
cd /home/ubuntu/realestate
# Изучить код
vim etl/collector_cian/browser_fetcher.py
# Реализовать исправление
# Написать тесты
# Протестировать
```

### Шаг 2: Фильтровать доли квартир
```bash
# Обновить payload
vim etl/collector_cian/payloads/cheap_first.yaml
# Добавить minArea: 20

# Очистить существующие
psql -h localhost -U realuser -d realdb -c \
  "UPDATE listings SET is_active = FALSE WHERE area_total < 20;"
```

### Шаг 3: Запустить созданные скрипты
```bash
# Read-only пользователь
./scripts/create_readonly_db_user.sh

# Система алертов
./scripts/setup_basic_alerts.sh

# SSL automation (если используется Nginx)
./scripts/setup_ssl_automation.sh realestate.ourdocs.org admin@example.com
```

---

## 📊 Текущий статус проекта

### ✅ Завершено (85%)
- Infrastructure: 100%
- Security: 100%
- Documentation: 100%
- Basic Monitoring: 33%

### 🟡 В процессе (10%)
- Data Quality: TASK-001, BUG-001
- Advanced Monitoring: TASK-PROD-016

### 📋 Планируется (5%)
- Performance: Connection pooling, Caching
- Scalability: Load balancer, Auto-scaling

---

## 🔗 Связанные документы

- `.speckit/tasks/current-sprint.md` - Текущий спринт
- `.speckit/tasks/PRODUCTION-DEPLOYMENT.md` - Production задачи
- `.speckit/bugs/BUG-001-shares-in-listings.md` - Баг с долями
- `RUNBOOK.md` - Операционное руководство
- `DISASTER_RECOVERY.md` - План восстановления

---

## 💡 Рекомендации

### Немедленно:
1. Исправить TASK-001 (критично для качества данных)
2. Исправить BUG-001 (фильтрация долей)
3. Запустить созданные скрипты автоматизации

### На этой неделе:
4. Настроить расширенный мониторинг
5. Добавить метрики качества данных
6. Настроить автоматический сбор данных

### В следующем месяце:
7. Оптимизация производительности
8. Масштабирование (если нужно)

---

**Последнее обновление:** 2025-11-19  
**Следующий review:** 2025-11-26

