import { HTMLAttributes, ReactNode } from 'react';
import { HorizontalDirection, Orientation } from '..';

export interface FormContainerProps extends HTMLAttributes<HTMLDivElement> {
    htmlFor: string;
    label?: ReactNode;
    orientation?: Orientation;
    dir?: HorizontalDirection;
    error?: ReactNode;
    children: ReactNode;
    addLabelPadding?: boolean;
}
export declare const FormContainer: ({ id, label, orientation, dir, error, children, addLabelPadding, ...props }: FormContainerProps) => import("react/jsx-runtime").JSX.Element;
