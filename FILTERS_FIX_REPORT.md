# Отчет об исправлении фильтров и сортировки

**Дата:** 2025-11-19  
**Проблема:** При применении фильтров или сортировки сайт падал с ошибкой 422

---

## 🔍 Проблема

При отправке формы с пустыми значениями фильтров (например, `rooms=&min_price=&max_price=`) FastAPI возвращал ошибку **422 Unprocessable Content**.

**Причина:**
- FastAPI ожидал параметры типа `Optional[int]` с валидацией `ge=1, le=5`
- Когда форма отправлялась с пустыми полями, они приходили как пустые строки `""`
- FastAPI не мог преобразовать пустую строку в `int`, что вызывало ошибку валидации

---

## ✅ Решение

### 1. Изменение типов параметров

**Было:**
```python
rooms: Optional[int] = Query(None, ge=1, le=5),
min_price: Optional[int] = Query(None, ge=0),
max_price: Optional[int] = Query(None, ge=0),
```

**Стало:**
```python
rooms: Optional[str] = Query(None),
min_price: Optional[str] = Query(None),
max_price: Optional[str] = Query(None),
```

### 2. Ручной парсинг и валидация

Добавлена функция парсинга строковых значений с обработкой пустых строк:

```python
# Parse and validate filter parameters (handle empty strings from form)
rooms_int = None
if rooms and rooms.strip():
    try:
        rooms_int = int(rooms.strip())
        if rooms_int < 1 or rooms_int > 5:
            rooms_int = None
    except ValueError:
        rooms_int = None

min_price_int = None
if min_price and min_price.strip():
    try:
        min_price_int = int(min_price.strip())
        if min_price_int < 0:
            min_price_int = None
    except ValueError:
        min_price_int = None

max_price_int = None
if max_price and max_price.strip():
    try:
        max_price_int = int(max_price.strip())
        if max_price_int < 0:
            max_price_int = None
    except ValueError:
        max_price_int = None
```

### 3. Исправление устаревшего параметра

**Было:**
```python
sort: str = Query("price_asc", regex="^...$")
```

**Стало:**
```python
sort: str = Query("price_asc", pattern="^...$")
```

---

## 📊 Результаты тестирования

### ✅ Тест 1: Пустые фильтры
```bash
GET /?rooms=&min_price=&max_price=&sort=price_desc&limit=50
```
**Результат:** ✅ 200 OK (ранее: 422 Unprocessable Content)

### ✅ Тест 2: Фильтр по комнатам
```bash
GET /?rooms=1&limit=5
```
**Результат:** ✅ 200 OK

### ✅ Тест 3: Сортировка
```bash
GET /?sort=area_desc&limit=5
```
**Результат:** ✅ 200 OK

### ✅ Тест 4: Комбинированные фильтры
```bash
GET /?rooms=2&min_price=10000000&max_price=20000000&sort=area_desc&limit=10
```
**Результат:** ✅ 200 OK

---

## 📝 Измененные файлы

- `web_viewer.py` - исправлена обработка параметров фильтров

---

## 🎯 Итог

**Статус:** ✅ **ИСПРАВЛЕНО**

- Фильтры работают корректно с пустыми значениями
- Сортировка работает без ошибок
- Все комбинации фильтров и сортировки протестированы
- Устранено предупреждение об устаревшем параметре `regex`

---

## 🔗 Ссылки

- **Веб-интерфейс:** https://realestate.ourdocs.org
- **Логи:** `sudo journalctl -u realestate-web -f`

